[project]
name = "voiceops-flight-mock"
version = "0.1.0"
description = "Mock airline servicing API with an injectable chaos engine"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "psycopg[binary,pool]>=3.2",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
]

[tool.ruff]
line-length = 100
